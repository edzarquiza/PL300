[
  {
    "id": "cs_fabrikam",
    "title": "Fabrikam Manufacturing Analytics",
    "scenario": "Fabrikam Industries is a global manufacturing company with 12,000 employees across 8 sales regions. Their BI team built a Power BI solution three years ago that has become the primary reporting tool for regional sales managers and the executive team.",
    "currentSetup": "The model uses a single denormalized fact table SalesActivity (18.5 million rows, 52 columns) in DirectQuery mode against an on-premises SQL Server 2019. All KPI calculations — Revenue, GrossMargin, YTD_Revenue, YTD_Margin, and QoQ_Growth — are implemented as calculated columns. All model relationships use bidirectional cross-filter. RLS is configured with a 'Region Manager' role: [SalesRegion] = LOOKUPVALUE(RegionAccess[Region], RegionAccess[ManagerEmail], USERPRINCIPALNAME()). Average report load time: 11–14 seconds.",
    "requirements": [
      "Reduce report load time to under 3 seconds without losing historical data access",
      "Ensure regional managers see only data for their assigned region — the current implementation has intermittent security gaps",
      "CFO requires a rolling 12-month revenue trend compared to the prior year on a monthly basis",
      "Reports must be accessible to 250 internal Pro-licensed users and 20 external distributors without Power BI licenses"
    ],
    "questions": [
      {
        "id": "cs_fabrikam_q1",
        "type": "single",
        "domain": "Model the data",
        "subtopic": "Optimize model performance",
        "difficulty": "Hard",
        "caseStudyId": "cs_fabrikam",
        "question": "Performance Analyzer shows YTD_Revenue and YTD_Margin calculated columns account for 9 of the 12 seconds of average load time. These columns are defined on the 18.5M-row SalesActivity table in DirectQuery mode. What is the MOST effective resolution?",
        "choices": [
          "Convert SalesActivity to Import mode and rewrite YTD_Revenue and YTD_Margin as DAX time intelligence measures using DATESYTD",
          "Add non-clustered indexes to the YTD and Margin columns in SQL Server to reduce formula engine computation",
          "Reduce the slicer date range to 24 months using a report-level filter to limit the rows processed",
          "Switch to DirectLake storage mode, which handles aggregation columns more efficiently than DirectQuery"
        ],
        "correctAnswers": [0],
        "explanation": "Calculated columns in DirectQuery mode are computed by the Formula Engine on every query — they are not stored in VertiPaq. With 18.5M rows, each YTD calculation iterates the full row set per visual refresh. Converting to Import mode stores data in compressed columnar storage, and rewriting as measures (evaluated on aggregated results at query time) reduces Formula Engine overhead by orders of magnitude. DirectLake requires OneLake/Fabric and cannot connect to on-premises SQL Server.",
        "trapType": "Import vs DirectQuery",
        "tags": ["Performance", "DirectQuery", "Import", "Calculated Columns", "Measures"]
      },
      {
        "id": "cs_fabrikam_q2",
        "type": "single",
        "domain": "Manage and secure Power BI",
        "subtopic": "Implement row-level security roles",
        "difficulty": "Hard",
        "caseStudyId": "cs_fabrikam",
        "question": "Regional managers intermittently see sales data from other regions. The RLS filter on SalesActivity is: [SalesRegion] = LOOKUPVALUE(RegionAccess[Region], RegionAccess[ManagerEmail], USERPRINCIPALNAME()). RegionAccess is a standalone table with no model relationships. The issue affects some managers but not others. What is the MOST likely cause?",
        "choices": [
          "LOOKUPVALUE returns BLANK when the manager's email is not found in RegionAccess — the expression [SalesRegion] = BLANK() then returns TRUE for rows where SalesRegion is null or empty, exposing those rows to unmatched managers",
          "USERPRINCIPALNAME() returns different values in Power BI Desktop versus the Power BI Service, causing some managers to bypass the filter",
          "The RegionAccess table must have a model relationship to SalesActivity for LOOKUPVALUE to evaluate correctly",
          "The RLS filter should use USERNAME() instead of USERPRINCIPALNAME() when the source is an on-premises database"
        ],
        "correctAnswers": [0],
        "explanation": "This is a critical RLS security trap. When LOOKUPVALUE finds no matching email, it returns BLANK. The expression [SalesRegion] = BLANK() evaluates to TRUE for any row where SalesRegion is empty or null — inadvertently exposing those rows to every manager whose email is absent from RegionAccess. The 'intermittent' pattern (some managers affected, others not) is explained by which managers' emails are missing. The fix: IF(ISBLANK(LOOKUPVALUE(...)), FALSE(), [SalesRegion] = LOOKUPVALUE(...)).",
        "trapType": "RLS LOOKUPVALUE Blank Vulnerability",
        "tags": ["RLS", "LOOKUPVALUE", "Security", "USERPRINCIPALNAME", "DAX"]
      },
      {
        "id": "cs_fabrikam_q3",
        "type": "single",
        "domain": "Manage and secure Power BI",
        "subtopic": "Choose a distribution method",
        "difficulty": "Medium",
        "caseStudyId": "cs_fabrikam",
        "question": "Fabrikam needs to distribute the regional sales report to 20 external distributors who do not have Power BI Pro licenses. The report contains commercially sensitive pricing data and distributors must not be able to export or download the underlying data. What is the MOST appropriate approach?",
        "choices": [
          "Publish the report to a Premium capacity workspace and distribute via a Power BI App to external Azure AD B2B guest accounts — configure Export and sharing restrictions in workspace settings",
          "Export the report as a PDF and send it via encrypted email on a monthly basis",
          "Assign the external distributors Viewer roles on the workspace and issue them free Fabric (free tier) licenses",
          "Publish the report with anonymous public access enabled and restrict access at the corporate network level"
        ],
        "correctAnswers": [0],
        "explanation": "Power BI Premium capacity allows external Azure AD B2B guest users to consume reports without per-user Pro licenses — the capacity license covers consumption. Distributing via a Power BI App provides controlled access, and Export restrictions can be enforced at workspace and tenant level. Viewer workspace roles (C) require Pro licenses in non-Premium workspaces. Anonymous access (D) bypasses all identity-based security controls. PDF export (B) is not a scalable operational solution.",
        "tags": ["Distribution", "Premium", "B2B", "Licensing", "Export Restrictions"]
      }
    ]
  },
  {
    "id": "cs_northwind",
    "title": "Northwind Financial Services Dashboard",
    "scenario": "Northwind Financial Services is a mid-sized investment management firm managing portfolios for 2,400 clients. The BI team is building a consolidated performance dashboard to replace manual Excel-based monthly reporting compiled from three separate source systems.",
    "currentSetup": "Data sources: (1) Azure SQL Database — client portfolio and transaction data, updated every 15 minutes. (2) SharePoint Online — weekly risk assessment Excel files uploaded by senior analysts. (3) On-premises SQL Server 2016 — legacy trade execution system, read-only BI access via data gateway, updated daily via overnight batch. Finance analysts currently export from all three sources, join in Excel, and email reports monthly. All data is subject to MiFID II compliance — client data must be labelled, audited, and access-controlled.",
    "requirements": [
      "Automate data refresh — portfolio data must reflect changes within 2 hours; risk assessments and trade data may refresh daily",
      "Senior analysts see all client data; junior analysts see only the portfolios assigned to them",
      "All reports containing client data must carry the 'Highly Confidential' sensitivity label automatically",
      "The solution must allow the BI team to manage access without IT involvement for routine access changes"
    ],
    "questions": [
      {
        "id": "cs_northwind_q1",
        "type": "single",
        "domain": "Prepare the data",
        "subtopic": "Choose between DirectLake, DirectQuery, and Import",
        "difficulty": "Hard",
        "caseStudyId": "cs_northwind",
        "question": "Northwind must combine data from Azure SQL Database (updates every 15 minutes), SharePoint Excel (weekly), and on-premises SQL Server (daily batch via gateway). Portfolio data must reflect changes within 2 hours. Which combination of storage modes is MOST appropriate?",
        "choices": [
          "Composite model — Import mode for SharePoint and on-premises SQL Server (scheduled daily refresh via on-premises gateway), DirectQuery for Azure SQL Database for near-real-time portfolio data",
          "Full Import mode for all three sources with a 2-hour scheduled refresh cycle applied to the entire model",
          "DirectQuery for all three sources to eliminate scheduled refresh dependency and ensure real-time access",
          "DirectLake for Azure SQL Database and Import mode for the remaining sources — DirectLake provides sub-minute latency for Azure-hosted data"
        ],
        "correctAnswers": [0],
        "explanation": "A composite model allows each table to use the most appropriate storage mode. Azure SQL (frequent updates) benefits from DirectQuery — always current without refresh overhead. SharePoint Excel (weekly) and on-premises SQL (daily batch) are stable enough for scheduled Import refresh via the on-premises data gateway. Full Import with 2-hour refresh (B) continuously stresses all three source systems. DirectLake (D) requires Microsoft Fabric/OneLake and cannot connect to arbitrary external databases or on-premises systems.",
        "trapType": "Import vs DirectQuery",
        "tags": ["Composite Model", "DirectQuery", "Import", "Data Gateway", "Storage Mode"]
      },
      {
        "id": "cs_northwind_q2",
        "type": "single",
        "domain": "Manage and secure Power BI",
        "subtopic": "Implement row-level security roles",
        "difficulty": "Hard",
        "caseStudyId": "cs_northwind",
        "question": "Junior analysts must see only client portfolios assigned to them. The model includes a ClientPortfolio fact table (ClientID, Revenue, RiskScore) and a separate PortfolioAssignment mapping table (AnalystUPN, ClientID) with no model relationship. Which RLS DAX filter applied to ClientPortfolio CORRECTLY restricts junior analysts to their assigned clients?",
        "choices": [
          "[ClientID] IN SELECTCOLUMNS(FILTER(PortfolioAssignment, [AnalystUPN] = USERPRINCIPALNAME()), \"CID\", [ClientID])",
          "USERPRINCIPALNAME() = [AnalystUPN]",
          "[ClientID] = LOOKUPVALUE(PortfolioAssignment[ClientID], PortfolioAssignment[AnalystUPN], USERPRINCIPALNAME())",
          "CALCULATE(COUNTROWS(FILTER(PortfolioAssignment, [AnalystUPN] = USERPRINCIPALNAME() && [ClientID] = EARLIER([ClientID])))) > 0"
        ],
        "correctAnswers": [0],
        "explanation": "The IN SELECTCOLUMNS(FILTER(...)) pattern correctly handles multi-value RLS from a disconnected mapping table — it returns the full set of ClientIDs assigned to the current user and filters ClientPortfolio to only those rows. LOOKUPVALUE (C) returns only the FIRST matching ClientID, so analysts managing multiple portfolios would see only one. Option B applies to the wrong table — ClientPortfolio has no AnalystUPN column. Option D uses EARLIER which is only valid inside calculated columns, not in RLS filter expressions.",
        "trapType": "RLS LOOKUPVALUE vs IN for multiple rows",
        "tags": ["RLS", "Dynamic Security", "USERPRINCIPALNAME", "FILTER", "SELECTCOLUMNS"]
      },
      {
        "id": "cs_northwind_q3",
        "type": "single",
        "domain": "Manage and secure Power BI",
        "subtopic": "Apply sensitivity labels",
        "difficulty": "Medium",
        "caseStudyId": "cs_northwind",
        "question": "Northwind's compliance requirement states all reports containing client financial data must carry the 'Highly Confidential' label. An analyst publishes a new report without applying the label. What is the MOST automated and scalable enforcement approach?",
        "choices": [
          "Configure mandatory sensitivity label policies in Microsoft Purview that set 'Highly Confidential' as the default label for Power BI content in the workspace and prevent saving without a label",
          "Instruct all workspace Admins to manually review and apply labels to newly published content within 24 hours of publication",
          "Enable downstream sensitivity label inheritance — reports automatically inherit the label from their source semantic model when the model carries 'Highly Confidential'",
          "Restrict publishing rights to workspace Admins only, who verify labels are applied before publishing on behalf of analysts"
        ],
        "correctAnswers": [2],
        "explanation": "Downstream label inheritance (C) is the most automated approach for this scenario. When the source semantic model is labelled 'Highly Confidential', all reports built from it automatically inherit that label — no analyst action required. This is the standard Power BI + Purview integration pattern. Mandatory Purview policies (A) are the strongest enforcement control but require additional compliance licensing and admin configuration. Manual admin review (B, D) introduces human error and does not scale to a BI team publishing frequently.",
        "tags": ["Sensitivity Labels", "Purview", "Compliance", "MiFID II", "Label Inheritance"]
      }
    ]
  },
  {
    "id": "cs_contoso_health",
    "title": "Contoso Healthcare Patient Analytics",
    "scenario": "Contoso Regional Health System operates 6 hospitals and 40 outpatient clinics across three states. Their analytics team is building a centralized Power BI solution to consolidate patient outcome metrics, operational throughput, and financial performance for clinical and administrative leaders.",
    "currentSetup": "Data is stored in an Azure Synapse Analytics dedicated SQL pool (patient encounters, procedures, billing — 220 million rows). Clinical reference data (ICD codes, provider rosters, facility mappings) is maintained in Excel workbooks on SharePoint. Refresh: the Synapse pool receives daily batch loads at 2:00 AM. All patient-identifiable information is classified as Protected Health Information (PHI) under HIPAA. Department heads should only see data for patients within their department. The executive team requires an enterprise-wide view. Current report build times in Power BI Desktop range from 45 seconds to 3 minutes per visual when connected via DirectQuery.",
    "requirements": [
      "Patient-level data must never be exposed to unauthorized users — department heads see only their department",
      "Report performance must allow executives to load a 6-hospital summary in under 5 seconds",
      "Source data changes daily — reports must reflect yesterday's data by 6:00 AM",
      "PHI fields must be masked or excluded from any export functionality"
    ],
    "questions": [
      {
        "id": "cs_contoso_q1",
        "type": "single",
        "domain": "Prepare the data",
        "subtopic": "Choose between DirectLake, DirectQuery, and Import",
        "difficulty": "Hard",
        "caseStudyId": "cs_contoso_health",
        "question": "The analytics team is evaluating storage modes for the 220M-row patient encounters table in Synapse Analytics. Executives report 45-second visual load times in DirectQuery mode. Refresh must reflect yesterday's data by 6:00 AM. Which storage mode configuration BEST meets both the performance and freshness requirements?",
        "choices": [
          "Import mode with incremental refresh policy — partition by EncounterDate, import only the last 3 days on each refresh cycle, retain 5 years historical in frozen partitions, schedule refresh at 3:00 AM",
          "DirectQuery mode with Synapse materialized views pre-aggregating the most common executive KPIs to eliminate Formula Engine overhead",
          "DirectLake mode pointing to the Synapse-backed OneLake delta tables for zero-ETL import with automatic framing at each batch load",
          "Composite model — DirectQuery for the current month's encounters for live data, Import for all prior months"
        ],
        "correctAnswers": [0],
        "explanation": "Import mode with incremental refresh is the standard PL-300 answer for large tables with daily batch loads and sub-second performance requirements. Incremental refresh partitions the table so only recent data is re-imported each cycle (here 3 days covers batch delays), while historical partitions remain frozen — avoiding a full 220M-row import daily. VertiPaq compression on Import mode delivers the sub-5-second executive load times. DirectQuery with materialized views (B) reduces load but cannot match Import compression ratios at 220M rows. DirectLake (C) requires Microsoft Fabric; Synapse dedicated SQL pools do not automatically expose OneLake delta tables without Fabric integration.",
        "trapType": "Import vs DirectQuery",
        "tags": ["Incremental Refresh", "Import Mode", "DirectQuery", "Performance", "Synapse"]
      },
      {
        "id": "cs_contoso_q2",
        "type": "single",
        "domain": "Manage and secure Power BI",
        "subtopic": "Implement row-level security roles",
        "difficulty": "Hard",
        "caseStudyId": "cs_contoso_health",
        "question": "The model has a DimDepartment table with a DepartmentHead_UPN column. Department heads must see only encounters where Encounter[DepartmentID] matches their assigned department. Executives belong to an 'Executive' Azure AD security group and must bypass department filtering. Which RLS design CORRECTLY implements this without creating separate roles for each department?",
        "choices": [
          "Create a single 'DeptHead' role with filter on DimDepartment: [DepartmentHead_UPN] = USERPRINCIPALNAME(). Add executives to a separate 'Executive' role with no filter. Assign users to exactly one role in the semantic model.",
          "Create a 'DeptHead' role with filter on the Encounter fact table: [DepartmentID] IN VALUES(DimDepartment[DepartmentID]). Use LOOKUPVALUE to retrieve the department of the current user from a separate HR table.",
          "Create one role with the DAX filter: IF(USERPRINCIPALNAME() IN VALUES(ExecRoster[UPN]), TRUE(), [DepartmentHead_UPN] = USERPRINCIPALNAME()). Apply the filter to DimDepartment.",
          "Create a dynamic role using USERNAME() on the Encounter table: [DepartmentID] = LOOKUPVALUE(DimDepartment[DepartmentID], DimDepartment[DepartmentHead_UPN], USERNAME())"
        ],
        "correctAnswers": [0],
        "explanation": "The correct pattern uses separate named roles — one for department heads filtered via DimDepartment (which propagates to Encounter via the relationship), and one for executives with no filter. Power BI applies the most permissive role when a user belongs to multiple roles, so executives assigned to the 'Executive' role see all data. This avoids the maintenance burden of per-department roles. Option C places the executive check inside a DAX IF expression — this pattern is not reliably supported in Power BI RLS and can produce unexpected results. Option D uses USERNAME() which returns domain\\user format on-premises, not UPN — unreliable in the Power BI Service.",
        "trapType": "RLS Role Design",
        "tags": ["RLS", "Dynamic Security", "USERPRINCIPALNAME", "Role Design", "PHI"]
      },
      {
        "id": "cs_contoso_q3",
        "type": "single",
        "domain": "Visualize and analyze data",
        "subtopic": "Configure export settings",
        "difficulty": "Medium",
        "caseStudyId": "cs_contoso_health",
        "question": "HIPAA compliance requires that PHI fields (PatientName, DateOfBirth, SSN_Last4) must never be exportable from Power BI reports, even by department heads who can view them on screen. The fields are visible in table and matrix visuals for clinical users. What is the MOST appropriate configuration?",
        "choices": [
          "In the Power BI Admin portal, disable 'Export data' for the workspace under tenant settings; supplement with sensitivity labels that restrict export on the semantic model",
          "Remove PatientName, DateOfBirth, and SSN_Last4 from all visuals and replace with a de-identified PatientToken column; retain PHI only in a separate restricted semantic model used by authorized clinical apps",
          "Set the semantic model sensitivity label to 'Highly Confidential — PHI' in Microsoft Purview and configure label-based protection to block download/export actions",
          "Configure workspace-level export restrictions to 'Allow export with restrictions' and instruct report authors to exclude PHI columns from table visuals"
        ],
        "correctAnswers": [1],
        "explanation": "The only reliable way to prevent PHI export is to exclude it from the model entirely for general reporting. If PHI is present in the semantic model, any user with Build permission or data export capability could access it regardless of visual-level hiding. De-identification at the model level (B) is the architecturally sound HIPAA-compliant approach — clinical detail is served through a separate, purpose-built restricted model. Sensitivity labels (C) add a governance layer but rely on Microsoft 365 label enforcement, which does not prevent all export paths. Admin portal export restrictions (A, D) are policy controls that can be bypassed by users with the right permissions or desktop tools.",
        "tags": ["PHI", "HIPAA", "Export Restrictions", "Data Security", "Sensitivity Labels"]
      }
    ]
  },
  {
    "id": "cs_adventure_works",
    "title": "Adventure Works Retail Modernization",
    "scenario": "Adventure Works is a sporting goods retailer with 180 stores across North America, a growing e-commerce channel, and a wholesale distribution arm. Their BI team is migrating from SSRS paginated reports to an interactive Power BI solution serving 600 store managers, 15 regional directors, and the executive board.",
    "currentSetup": "Source: on-premises SQL Server 2019 data warehouse with a partially normalized star schema. The FactSales table has 95 million rows. DimCustomer has a Type 2 SCD (historical rows with IsCurrent flag). The team currently runs 3 separate SQL queries to produce a monthly P&L: one for store sales, one for e-commerce, and one for wholesale. Each query returns different grain (store/day, order/hour, invoice/month). Marketing wants a DAX measure that shows 'Sales vs Same Period Last Year' with the ability to slice by store, product category, and channel.",
    "requirements": [
      "Unify store, e-commerce, and wholesale sales into a single fact table at day-channel-store grain without losing order-level e-commerce detail",
      "The SCD Type 2 DimCustomer must correctly attribute sales to the customer profile active at the time of sale",
      "The 'Sales vs Same Period Last Year' measure must work correctly with any combination of date, store, category, and channel slicers",
      "Regional directors must see all stores in their region; store managers see only their own store"
    ],
    "questions": [
      {
        "id": "cs_aw_q1",
        "type": "single",
        "domain": "Prepare the data",
        "subtopic": "Merge and append queries",
        "difficulty": "Hard",
        "caseStudyId": "cs_adventure_works",
        "question": "The team needs to combine store sales (day-store grain), e-commerce orders (hourly), and wholesale invoices (monthly) into a single FactSales table. Store and e-commerce data share the same SalesDate, StoreID, ProductID, and Revenue columns. Wholesale invoices have InvoiceDate, DistributorID, ProductID, and InvoiceRevenue with no StoreID. Which Power Query approach BEST supports a unified model without distorting granularity?",
        "choices": [
          "Append store and e-commerce queries (matching columns), add a Channel column to each before appending; load wholesale as a separate FactWholesale table linked to a shared DimProduct and DimDate — avoid forcing into FactSales due to grain mismatch",
          "Merge all three queries using a full outer join on ProductID and date, filling nulls for missing columns with 0 to ensure no rows are dropped",
          "Append all three sources and add a grain identifier column; aggregate to day-store grain using Group By on SalesDate and StoreID before loading",
          "Create a reference query from FactSales, filter by Channel, and use Merge to add wholesale data via a left outer join on ProductID"
        ],
        "correctAnswers": [0],
        "explanation": "When combining datasets with different grains, forcing them into the same fact table introduces data integrity risks. Appending store and e-commerce (same day-store grain) is valid. Wholesale at invoice-month grain with no StoreID cannot be cleanly appended — DistributorID does not map to StoreID, and aggregating to day would fabricate sub-invoice data. A separate FactWholesale table sharing DimProduct and DimDate allows accurate analysis at each table's natural grain. A full outer join (B) on ProductID alone is meaningless — one product can have millions of sales rows. Forced aggregation (C) loses the e-commerce hourly detail that was specified as a requirement.",
        "trapType": "Merge vs Append",
        "tags": ["Append", "Merge", "Fact Table Grain", "Power Query", "Data Integration"]
      },
      {
        "id": "cs_aw_q2",
        "type": "single",
        "domain": "Model the data",
        "subtopic": "Create model calculations by using DAX",
        "difficulty": "Hard",
        "caseStudyId": "cs_adventure_works",
        "question": "The marketing team wants a measure [Sales vs SPLY %] that compares current period sales to the same period last year, working correctly with any combination of date, store, product category, and channel slicers. The model has a standard DimDate table marked as a date table with a DateKey column. Which DAX measure is MOST correct?",
        "choices": [
          "Sales vs SPLY % = DIVIDE([Total Sales] - CALCULATE([Total Sales], SAMEPERIODLASTYEAR(DimDate[Date])), CALCULATE([Total Sales], SAMEPERIODLASTYEAR(DimDate[Date])))",
          "Sales vs SPLY % = DIVIDE([Total Sales] - CALCULATE([Total Sales], DATEADD(DimDate[Date], -1, YEAR)), CALCULATE([Total Sales], DATEADD(DimDate[Date], -1, YEAR)))",
          "Sales vs SPLY % = DIVIDE([Total Sales], CALCULATE([Total Sales], SAMEPERIODLASTYEAR(DimDate[Date]))) - 1",
          "Sales vs SPLY % = ([Total Sales] - [Total Sales LY]) / [Total Sales LY]"
        ],
        "correctAnswers": [2],
        "explanation": "Option C is the most concise and correct form: DIVIDE(Current, PY) - 1 gives the percentage change. SAMEPERIODLASTYEAR respects the current date filter context from all slicers (date, store, category, channel) and is the standard PL-300 time intelligence function for this pattern. Option A is logically equivalent but unnecessarily calculates SAMEPERIODLASTYEAR twice (no variable reuse). Option B using DATEADD -1 YEAR is functionally similar but SAMEPERIODLASTYEAR is preferred for this exact use case. Option D references [Total Sales LY] as if it were a separate named measure — if that measure is undefined, the formula breaks; this pattern should be self-contained or use VAR.",
        "trapType": "Time Intelligence function selection",
        "tags": ["SAMEPERIODLASTYEAR", "Time Intelligence", "DIVIDE", "Measures", "DAX"]
      },
      {
        "id": "cs_aw_q3",
        "type": "single",
        "domain": "Model the data",
        "subtopic": "Implement role-playing dimensions",
        "difficulty": "Medium",
        "caseStudyId": "cs_adventure_works",
        "question": "FactSales has two date foreign keys: OrderDate and ShipDate. Both need to relate to DimDate for time intelligence. The team creates two relationships between FactSales and DimDate, but Power BI only allows one to be active. A measure [Sales by Ship Date] must correctly use the ShipDate relationship for SAMEPERIODLASTYEAR. How should this be implemented?",
        "choices": [
          "Keep the OrderDate → DimDate relationship active. Create [Sales by Ship Date] using CALCULATE([Total Sales], USERELATIONSHIP(FactSales[ShipDate], DimDate[DateKey])). Wrap the SAMEPERIODLASTYEAR call inside the same CALCULATE.",
          "Create a second copy of DimDate named DimShipDate and establish a separate active relationship from FactSales[ShipDate] to DimShipDate[DateKey]",
          "Mark both relationships as active — Power BI will use the relationship that matches the slicer's connected date dimension",
          "Add a ShipYear, ShipMonth calculated column to FactSales using RELATED(DimDate[Year]) to avoid needing a second relationship"
        ],
        "correctAnswers": [0],
        "explanation": "Role-playing dimensions are handled in Power BI by keeping one relationship active and activating the inactive relationship within a CALCULATE using USERELATIONSHIP. The SAMEPERIODLASTYEAR function must be inside the same CALCULATE that activates the USERELATIONSHIP — both modifiers apply within that filter context scope. A duplicate DimDate table (B) is the alternative approach and works correctly, but USERELATIONSHIP is the preferred PL-300 pattern when a single dimension serves multiple roles. Power BI does not allow two active relationships between the same tables (C is invalid). Calculated columns via RELATED (D) would return the OrderDate dimension values, not ShipDate.",
        "trapType": "Role-Playing Dimensions",
        "tags": ["USERELATIONSHIP", "Role-Playing Dimensions", "Time Intelligence", "Inactive Relationships", "DAX"]
      }
    ]
  },
  {
    "id": "cs_tailspin",
    "title": "Tailspin Toys Global Distribution",
    "scenario": "Tailspin Toys is a global toy manufacturer and distributor operating in 22 countries. Their Power BI solution serves 800 users including country managers, category leads, and a global supply chain team. The solution was built 18 months ago and is showing significant performance and governance issues as the business has scaled.",
    "currentSetup": "The model imports data from three sources: Azure SQL (global orders, 140M rows), Azure Blob Storage (product master CSV files, refreshed ad-hoc by the product team), and a third-party logistics API (delivery status, JSON format, updated every 6 hours). The model has 14 measures, 31 calculated columns on the FactOrders table, and 6 bidirectional relationships. Workspace: shared Premium P1 capacity. Users report 8–15 second load times. The product CSV files occasionally have schema changes (new columns added) that break the refresh pipeline. Country managers have raised concerns that they sometimes see order data from other countries.",
    "requirements": [
      "Reduce average report load time to under 4 seconds",
      "Prevent CSV schema changes from breaking the refresh pipeline",
      "Ensure country managers see only their country's orders — investigate and fix the data leakage",
      "Allow the product team to update product master data without IT involvement"
    ],
    "questions": [
      {
        "id": "cs_tailspin_q1",
        "type": "single",
        "domain": "Model the data",
        "subtopic": "Optimize model performance",
        "difficulty": "Hard",
        "caseStudyId": "cs_tailspin",
        "question": "Performance Analyzer shows the FactOrders table accounts for 73% of model memory. The table has 140M rows, 31 calculated columns, and 6 bidirectional relationships. A DAX query on a top-10 country revenue visual takes 11 seconds. Which combination of changes will MOST effectively reduce model size and query time?",
        "choices": [
          "Convert calculated columns that are used only in visuals (not in relationships or other measures) to measures; remove bidirectional relationships where single-direction filtering is sufficient; verify column data types are minimal (Integer vs Text for ID columns)",
          "Switch FactOrders to DirectQuery mode to eliminate in-memory storage; this moves calculation to the Azure SQL engine which is optimized for 140M-row aggregation",
          "Add aggregation tables in Power Query that pre-aggregate FactOrders to Country-Month grain; load both the aggregation table and detail table, configuring the aggregation table as a dual-storage model",
          "Reduce the import date range from all history to the last 24 months using a query parameter; archive older data in a separate historical model"
        ],
        "correctAnswers": [0],
        "explanation": "Calculated columns are stored in memory per row — 31 columns on a 140M-row table creates massive VertiPaq pressure and causes slow compression scans. Converting display-only calculated columns to measures eliminates this overhead (measures compute at query time on aggregated data, not row-by-row). Bidirectional relationships force both-direction filter propagation which multiplies query complexity — removing unnecessary bidirectional filters significantly reduces DAX query plan branching. Correct data types (Integer IDs vs Text) dramatically improve VertiPaq dictionary compression. DirectQuery (B) eliminates in-memory storage but introduces network round-trips and typically increases, not decreases, load time for aggregation-heavy executive dashboards.",
        "trapType": "Calculated Column vs Measure",
        "tags": ["Performance", "Calculated Columns", "Measures", "Bidirectional Relationships", "VertiPaq"]
      },
      {
        "id": "cs_tailspin_q2",
        "type": "single",
        "domain": "Prepare the data",
        "subtopic": "Create and modify parameters",
        "difficulty": "Medium",
        "caseStudyId": "cs_tailspin",
        "question": "The product team uploads updated product master CSV files to Azure Blob Storage. When the product team adds new columns to the CSV (e.g., a new 'EcoRating' attribute), the Power Query step that selects specific columns fails because the step references columns by name using Table.SelectColumns with a hardcoded list. The refresh pipeline errors with 'column not found'. What is the MOST resilient fix?",
        "choices": [
          "Replace the Table.SelectColumns step with Table.SelectColumns that dynamically builds the column list using only columns confirmed to exist: Table.SelectColumns(Source, List.Intersect({requiredCols}, Table.ColumnNames(Source)))",
          "Add a try/otherwise expression around the Table.SelectColumns step to silently ignore column errors and return the last successful refresh result",
          "Set the CSV source query to use schema-on-read inference disabled so Power Query accepts any schema without validation",
          "Create a Power Query parameter 'CSVSchema' that the product team updates manually each time they change the CSV structure before triggering refresh"
        ],
        "correctAnswers": [0],
        "explanation": "The root cause is a brittle column selection step that assumes a fixed schema. The resilient fix is to intersect the required column list with the actual column names available in the source — Table.SelectColumns only requests columns that exist. This allows the product team to add new columns to the CSV without breaking the pipeline; the Power Query step simply ignores columns it doesn't need. try/otherwise (B) would mask legitimate errors and could serve stale data silently — dangerous for a production pipeline. Schema-on-read disabling (C) is not a configurable option in this way. A manual parameter (D) defeats the goal of IT-free product team updates and is error-prone.",
        "trapType": "Power Query schema brittleness",
        "tags": ["Power Query", "Parameters", "Schema", "Resilient Pipelines", "M Language"]
      },
      {
        "id": "cs_tailspin_q3",
        "type": "single",
        "domain": "Manage and secure Power BI",
        "subtopic": "Implement row-level security roles",
        "difficulty": "Hard",
        "caseStudyId": "cs_tailspin",
        "question": "Country managers report seeing order data from other countries. The model has a 'CountryManager' RLS role with filter on DimCountry: [CountryCode] = USERPRINCIPALNAME(). Investigation shows FactOrders has a bidirectional relationship with DimProduct, and DimProduct has an active relationship with a PromotionFact table. Promotions reference orders from multiple countries. What is the MOST likely cause of the data leakage and the correct fix?",
        "choices": [
          "Bidirectional cross-filtering on the FactOrders ↔ DimProduct relationship allows filter context to propagate from PromotionFact through DimProduct back into FactOrders, bypassing the DimCountry RLS filter. Fix: change the FactOrders ↔ DimProduct relationship to single-direction (DimProduct → FactOrders).",
          "The RLS filter [CountryCode] = USERPRINCIPALNAME() is comparing a country code (e.g., 'US') against an email UPN — the comparison always evaluates to FALSE for all users, so no filter is applied and all rows are visible.",
          "The 'CountryManager' role has not been assigned to users in the Power BI Service — unassigned roles do not restrict access and default to showing all data.",
          "DimCountry uses a text CountryCode column but FactOrders stores a numeric CountryID — the relationship is on mismatched data types, so the filter does not propagate correctly."
        ],
        "correctAnswers": [0],
        "explanation": "This is a classic bidirectional relationship RLS leakage scenario. With bidirectional cross-filter on FactOrders ↔ DimProduct, filter context can flow FROM FactOrders TO DimProduct AND back. When PromotionFact is filtered, it can pull DimProduct rows from other countries' orders, and the bidirectional relationship then exposes those FactOrders rows — bypassing the DimCountry RLS filter that only directly filters DimCountry, not FactOrders through this cross-filter path. The fix is to revert to single-direction filtering where possible and use CROSSFILTER(FactOrders[ProductID], DimProduct[ProductID], NONE) in measures that need the reverse direction. Option B is plausible but if the filter always returned FALSE, NO data would be visible — the symptom is too much data, not too little.",
        "trapType": "Bidirectional Relationships and RLS",
        "tags": ["RLS", "Bidirectional Relationships", "Security", "Cross-filter", "Data Leakage"]
      }
    ]
  }
]
